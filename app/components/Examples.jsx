var React = require('react');

const Examples = (props) => {
  return (
    <div>
      <h4>Examples component</h4>
      <p className="welcome-text">Welcome to examples page!</p>
    </div>
  );
};

module.exports = Examples;
